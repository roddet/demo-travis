language: node_js
node_js:
  - "12"
before_script:
  - cd testeee
  - npm install
script: npm run build
deploy:
  local-dir: www
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master
notifications:
  email: false
